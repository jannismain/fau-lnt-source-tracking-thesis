\usepackage[dvipsnames]{xcolor} % import before tikz, pstricks
\usepackage{graphicx}

% FONTS
\usepackage[utf8]{luainputenc} % to be used with LuaLaTeX
\usepackage{fontspec}
\usepackage[T1]{fontenc}  % this breaks matlab code's 
%\textquotesingle
%\usepackage[utf8]{inputenc} % to be used with pdfLaTeX 


% LINKS
\usepackage{cleveref}  
\crefname{algorithm}{algorithm}{algorithms}
\usepackage{hyperref}
\newcommand{\algorithmautorefname}{Algorithm}
\renewcommand{\chapterautorefname}{Chapter}
\renewcommand{\sectionautorefname}{Section}
\renewcommand{\subsectionautorefname}{Subsection}
\usepackage{varioref}  % for references like "...Figure 5.4 on page 24."

% bibliography
\usepackage[backend=biber,style=ieee,natbib=true,giveninits=true,doi=false]{biblatex} % also: style=ieee[1], style=numeric-comp ([1-3]), style=authoryear-comp, style=alphabetic
%\usepackage{IEEEtrantools}  % needed to edit IEEE citations style

% captions for figures
\usepackage{caption}
\usepackage{sidecap}  % captions next to figures
\usepackage{subcaption}  % multiple captions per figure

% localisation / multilingual files
\usepackage{iflang}
\usepackage[english]{babel}
\usepackage{csquotes} % required by polyglossia/babel
\selectlanguage{english}

% better math input
\usepackage{mathtools}  % this also loads amsmath
\usepackage{bm} % to be able to have bold mathematical symbols
\usepackage{amssymb} % to be able to use \mathbb

% format algorithm summaries
\usepackage{algorithm}
\usepackage{algpseudocode}

% include files without using 'include'
\usepackage{import}  % circumvent "nested include" error  

% plot figures and matlab output
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{pgfplotstable}
\pgfplotsset{compat=newest}  % only compat=1.8 solved compilation issues
\usepgfplotslibrary{groupplots}
\usetikzlibrary{plotmarks}
\usetikzlibrary{arrows.meta}
\usetikzlibrary{chains,positioning,shapes.symbols}
\usetikzlibrary{shapes}
\usetikzlibrary{trees}
\usetikzlibrary{calc}
\usepgfplotslibrary{patchplots}
\input{plots/pgfplotscustomlegend}
% For now, externalize does not work. Fails to create PDF. Maybe I have macros between in tikzpicture-environment, that cannot get expanded!
%\usepgfplotslibrary{external}
%\tikzexternalize[prefix=data/externalized/]

% for automatically generated List of Abbreviations
% add 'nonumberlist,' to options to see, how often you used acronym (remove ones only used once!)
% add 'sort=use' for sorting by first use
\usepackage[acronym,toc,nopostdot,nonumberlist,style=super,nogroupskip,ucmark]{glossaries}
\setlength{\glsdescwidth}{0.8\linewidth}  % prevent lineb

% Display matlab code
%\usepackage[numbered,autolinebreaks,useliterate]{mcode}

% POSITIONING OF FLOATS
\usepackage{float}  % provides [H] option to stop floating environments from floating
\usepackage{longtable}  % allow for tables to break across pages
\setlength{\LTleft}{0pt}  % move tables flush to the left side
\usepackage[section]{placeins}  % provides \FloatBarrier (automatically after section)

% Profiling of LaTeX Code
%\usepgflibrary{profiler}
%\pgfprofilenewforenvironment{tikzpicture}
%\pgfprofilenewforcommand{\pgfkeys}{1}

\usepackage{booktabs}  % required by pandas.DataFrame.to_latex() 
\usepackage{tabularx}  % required for automatically linebreaking tables

\usepackage{enumitem}
\usepackage{grffile}
\usepackage{standalone}
\usepackage{chngcntr}  % required to reset chapter-wise counters
\usepackage{comment}  % use certain environments to make comments that can either be printed or excluded from print

\usepackage{etoolbox}  % use boolean toggles for conditional branching

