\begin{tikzpicture}
  \tikzset{
    block/.style={
      draw,
      minimum height=1cm,
      inner sep=1em,
    },
    arrow/.style={thick, ->, >=stealth}
    }
  \begin{scope}[start chain=transition going right,node distance=1cm]
    \node (sim)[block,on chain, minimum width=1cm] {simulate};
    \node (stft)[block,on chain, minimum width=1cm] {stft};
    \node (em)[block,on chain,minimum width=1cm] {em-algorithm};
    \node (loc)[block,on chain,minimum width=1cm] {est. location};
    \node (err)[block,on chain,minimum width=1cm] {est. error};
    
    \draw [arrow] (sim) -- node[anchor=west] {} (stft);
    \draw [arrow] (stft) -- node[anchor=west] {} (em);
    \draw [arrow] (em) -- node[anchor=west] {} (loc);
    \draw [arrow] (loc) -- node[anchor=west] {} (err);
  \end{scope}
\end{tikzpicture}